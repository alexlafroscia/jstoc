{
  "extends": "@tsconfig/node24/tsconfig.json",
  "compilerOptions": {
    "types": ["node"],

    "rootDir": "../src",

    // Allow importing `.ts` files
    // `src` will rewrite to `.js` in build output
    // `test` will import `.ts` directly
    "allowImportingTsExtensions": true
  }
}
