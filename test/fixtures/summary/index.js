/**
 * A long-winded description that the README should not repeat.
 *
 * @summary The whole summary tag is used.
 */
export function tagged() {}

/**
 * Only the first sentence appears. The rest of the paragraph does not.
 */
export function punctuated() {}

/**
 * A description that wraps onto
 * a second line without punctuation
 *
 * More detail that should not appear.
 */
export function unpunctuated() {}
